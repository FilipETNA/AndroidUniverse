package etna.androiduniverse.interfaces;

import android.view.View;

import etna.androiduniverse.entities.PlaceInfo;

public interface OnPlaceClickListener {
    void onPlaceClick(View v, PlaceInfo placeInfo);
}
